//Array shift method in ts

var arr = [11, 89, 23, 7, 98];
var first = arr.shift();
console.log(first); //Output: 11

// In the above example, the shift() method removes the first element from the array and returns it. The original array arr is changed after the shift() method is called.