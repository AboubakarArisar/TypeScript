let fruit: string[] = ['Apple', 'Orange', 'Banana']; 

for(var index in fruits) 
{ 
	console.log(fruit[index]); // output: Apple Orange Banana 
} 

for(var i = 0; i < fruit.length; i++) 
{ 
	console.log(fruit[i]); // output: Apple Orange Banana 
}
